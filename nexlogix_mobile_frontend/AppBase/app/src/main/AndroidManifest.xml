<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  xmlns:tools="http://schemas.android.com/tools">
	<uses-permission android:name="android.permission.INTERNET" />
	<application
			android:allowBackup="true"
			android:dataExtractionRules="@xml/data_extraction_rules"
			android:fullBackupContent="@xml/backup_rules"
			android:label="@string/app_name"
			android:supportsRtl="true"
			android:theme="@style/Theme.AppInterface"
			tools:targetApi="31">
		<!-- primera Actividad -->
		<activity
				android:name=".MainActivity"
				android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>

		<!-- Actividad conductor -->
		<activity android:name=".DriverActivity"
				  android:exported="true">
		</activity>

		<!-- Actividad reporte externo -->
		<activity android:name=".ReporteExternoActivity"
				  android:exported="true">
		</activity>

		<!-- Actividad reportes internos -->
		<activity android:name=".ReportesInternosActivity"
				  android:exported="true">
		</activity>
	</application>

</manifest>